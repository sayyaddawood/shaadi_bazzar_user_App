export type LoginFormType = {
  phone: string;
};

export type RegisterFormType = {
  name: string;
  city: string;
};

export type SendMessageFormType = {
  name: string;
  phone: string;
  email: string;
  date: string;
  details: string;
};
